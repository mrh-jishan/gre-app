%p#notice= notice
%h1 Vocabulary Sets
%table.table.table-striped
  %thead
    %tr
      %th Study plan
      %th Set name
      %th Count
      %th{:colspan => "3"}
  %tbody
    - @vocabulary_sets.each do |vocabulary_set|
      %tr
        %td= vocabulary_set.study_plan_id
        %td= vocabulary_set.set_name
        %td= vocabulary_set.is_completed
        %td= vocabulary_set.vocabularies_count
        %td= link_to 'Show', [@study_plan, vocabulary_set], :class => "btn btn-sm btn-info"
        %td= link_to 'Edit', edit_study_plan_vocabulary_set_path(@study_plan, vocabulary_set), :class => "btn btn-sm btn-info"
        %td= link_to 'Destroy', [@study_plan, vocabulary_set], method: :delete, data: {confirm: 'Are you sure?'}, :class => "btn btn-sm btn-info"
%br/
= link_to 'New Vocabulary Set', new_study_plan_vocabulary_set_path, :class => "btn btn-sm btn-info"
= link_to 'Study Plan', study_plans_path, :class => "btn btn-sm btn-info"
= link_to 'Quiz', study_plan_quizzes_path(@study_plan), :class => "btn btn-sm btn-info"
%td= link_to 'Vocabularies', vocabularies_path, :class => "btn btn-sm btn-info"
